# 🛡️ VectraVPN Landing Page

Премиальный лендинг для VPN-сервиса с современным дизайном, анимациями и адаптивной вёрсткой.

![License](https://img.shields.io/badge/license-MIT-purple)
![React](https://img.shields.io/badge/React-19-61DAFB)
![Tailwind](https://img.shields.io/badge/Tailwind-3.4-38B2AC)
![Vite](https://img.shields.io/badge/Vite-7-646CFF)

---

## 📋 Содержание

- [Стек технологий](#-стек-технологий)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Разработка](#-разработка)
- [Изменение контента](#-изменение-контента)
- [Кастомизация стилей](#-кастомизация-стилей)
- [Сборка для продакшена](#-сборка-для-продакшена)
- [Деплой](#-деплой)

---

## 🚀 Стек технологий

| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19 | UI библиотека |
| **Vite** | 7.x | Сборщик и dev-сервер |
| **Tailwind CSS** | 3.4 | Utility-first CSS фреймворк |
| **Framer Motion** | 12.x | Анимации |
| **Lucide React** | 0.5x | Иконки |
| **GSAP** | 3.x | Продвинутые анимации |

---

## 📁 Структура проекта

```
vectravpn.com/
├── public/                 # Статические файлы
│   ├── favicon.svg         # Иконка сайта
│   ├── robots.txt          # Правила для поисковиков
│   └── sitemap.xml         # Карта сайта
├── src/
│   ├── components/         # Переиспользуемые компоненты
│   │   ├── Button.jsx      # Кнопка с вариантами (primary/secondary)
│   │   ├── CustomCursor.jsx# Кастомный курсор
│   │   ├── LoadingScreen.jsx# Экран загрузки
│   │   ├── Navbar.jsx      # Навигация с мобильным меню
│   │   ├── ScrollProgress.jsx# Индикатор прокрутки
│   │   └── SectionTitle.jsx# Заголовок секции
│   ├── sections/           # Секции лендинга
│   │   ├── Hero.jsx        # Главный экран
│   │   ├── Features.jsx    # Функции VPN (6 карточек)
│   │   ├── WhyUs.jsx       # Преимущества (4 карточки)
│   │   ├── Pricing.jsx     # Тарифы (3 плана)
│   │   ├── Download.jsx    # Скачивание (6 платформ)
│   │   ├── FAQ.jsx         # Частые вопросы
│   │   └── Footer.jsx      # Подвал сайта
│   ├── hooks/              # React хуки
│   │   ├── useTheme.js     # Переключение темы (dark/light)
│   │   ├── useLocale.jsx   # Локализация (EN/RU)
│   │   ├── useSmoothScroll.js# Плавная прокрутка
│   │   └── useScrollProgress.js# Прогресс прокрутки
│   ├── locales/            # Переводы
│   │   ├── en.js           # Английский
│   │   └── ru.js           # Русский
│   ├── data/
│   │   └── vpn.js          # Данные (ссылки, статистика)
│   ├── App.jsx             # Главный компонент
│   ├── main.jsx            # Точка входа
│   └── index.css           # Глобальные стили и переменные
├── index.html              # HTML шаблон
├── package.json            # Зависимости
├── tailwind.config.js      # Конфиг Tailwind
├── postcss.config.js       # Конфиг PostCSS
├── vite.config.js          # Конфиг Vite
└── nginx.conf              # Конфиг Nginx для продакшена
```

---

## ⚡ Быстрый старт

### Требования
- Node.js 18+ (рекомендуется 20+)
- npm или yarn

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/CyberScoper/vectra-landing.git
cd vectra-landing

# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:5173`

---

## 🔧 Разработка

### Команды

```bash
npm run dev      # Запуск dev-сервера с hot reload
npm run build    # Сборка для продакшена
npm run preview  # Превью production сборки
npm run lint     # Проверка кода ESLint
```

---

## 📝 Изменение контента

### Тексты и переводы

Все тексты находятся в папке `src/locales/`:

**Английский**: `src/locales/en.js`
**Русский**: `src/locales/ru.js`

#### Пример изменения заголовка Hero:

```javascript
// src/locales/ru.js
export default {
  hero: {
    badge: 'Доверяют более 10M пользователей',
    title: 'Приватность без',           // ← Изменить тут
    titleHighlight: 'Компромиссов',     // ← Градиентный текст
    subtitle: 'Молниеносный VPN...',    // ← Описание
    cta: 'Попробовать бесплатно',       // ← Кнопка
    secondary: 'Смотреть тарифы',
  },
  // ...
}
```

### Тарифы (Pricing)

Тарифы настраиваются в `src/locales/ru.js` (и `en.js`):

```javascript
pricing: {
  plans: [
    {
      name: 'Basic',
      description: 'Для личного использования',
      monthlyPrice: 9.99,    // ← Цена за месяц
      yearlyPrice: 4.99,     // ← Цена при годовой оплате
      features: [            // ← Список функций
        '1 устройство',
        '60+ локаций',
        // ...
      ],
    },
    {
      name: 'Pro',
      popular: true,         // ← Пометка "Популярный"
      // ...
    },
  ],
}
```

### Функции VPN (Features)

Тексты в `src/locales/ru.js`:

```javascript
features: {
  title: 'Почему VectraVPN',
  items: [
    {
      title: 'Военное шифрование',          // ← Заголовок
      description: 'AES-256 шифрование...',  // ← Описание
    },
    // ... 6 карточек
  ],
}
```

**Иконки** задаются в файле `src/sections/Features.jsx`:

```javascript
import { Shield, Globe, Zap, ShieldOff, Eye, Smartphone } from 'lucide-react'

const featureIcons = [Shield, Globe, Zap, ShieldOff, Eye, Smartphone]
```

Все доступные иконки: https://lucide.dev/icons

### FAQ

Вопросы и ответы в `src/locales/ru.js`:

```javascript
faq: {
  items: [
    {
      question: 'Что такое VPN?',
      answer: 'VPN (Virtual Private Network)...',
    },
    // ... добавить ещё вопросы
  ],
}
```

### Статистика (Hero section)

Цифры в `src/data/vpn.js`:

```javascript
export const stats = [
  { value: '10M+', label: 'Users Worldwide' },
  { value: '60+', label: 'Server Locations' },
  { value: '99.9%', label: 'Uptime' },
  { value: '24/7', label: 'Support' },
]
```

### Социальные сети

В `src/data/vpn.js`:

```javascript
export const socialLinks = [
  { name: 'Twitter', url: 'https://twitter.com/vectravpn', icon: 'Twitter' },
  { name: 'Discord', url: 'https://discord.gg/vectravpn', icon: 'MessageCircle' },
  // ...
]
```

---

## 🎨 Кастомизация стилей

### Цветовая схема

Все цвета в `src/index.css`:

```css
:root {
  /* Основной фон */
  --bg-primary: #050510;
  --bg-secondary: #0a0a1a;
  --bg-card: #0d0d20;
  
  /* Текст */
  --text-primary: #ffffff;
  --text-secondary: #a1a1cc;
  
  /* Акцентные цвета (фиолетовый + циан) */
  --accent-primary: #a855f7;    /* ← Основной акцент */
  --accent-secondary: #06b6d4;  /* ← Вторичный акцент */
  
  /* Рамки */
  --border-color: #1f1f3f;
}
```

### Изменить акцентный цвет

Чтобы изменить фиолетовый на другой цвет, замените `--accent-primary`:

```css
/* Зелёный */
--accent-primary: #22c55e;

/* Синий */
--accent-primary: #3b82f6;

/* Розовый */
--accent-primary: #ec4899;
```

### Шрифты

Шрифты подключены в `index.html`:

```html
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300..700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
```

Изменить в `src/index.css`:

```css
:root {
  --font-sans: 'Space Grotesk', system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

---

## 📦 Сборка для продакшена

```bash
# Создать production build
npm run build

# Файлы появятся в папке dist/
```

Структура dist/:
```
dist/
├── index.html
├── assets/
│   ├── index-xxx.js      # Основной бандл (~350 KB)
│   ├── index-xxx.css     # Стили (~25 KB)
│   └── ...               # Lazy-loaded чанки
├── favicon.svg
├── robots.txt
└── sitemap.xml
```

---

## 🌐 Деплой

### Nginx (рекомендуется)

1. Скопировать build на сервер:
```bash
scp -r dist/* user@server:/var/www/vectravpn.com/
```

2. Настроить права:
```bash
sudo chown -R www-data:www-data /var/www/vectravpn.com
sudo chmod -R 755 /var/www/vectravpn.com
```

3. Использовать конфиг `nginx.conf` из репозитория:
```bash
sudo cp nginx.conf /etc/nginx/sites-available/vectravpn.com
sudo ln -s /etc/nginx/sites-available/vectravpn.com /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### SSL сертификат (Let's Encrypt)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d vectravpn.com -d www.vectravpn.com
```

### Vercel / Netlify

Просто подключите репозиторий — сборка произойдёт автоматически.

---

## 📄 Лицензия

MIT License — свободное использование.

---

## 🤝 Поддержка

Telegram: [@your_telegram](https://t.me/your_telegram)
Email: support@vectravpn.com

---

Сделано с 💜 для VectraVPN
